  <%= turbo_frame_tag "project" do %>
<% if user_signed_in? %>
    <h1>
      New Project
    </h1>
    <div class="modal-body">
      <%= render "form", project: @project %>
    </div>
	</div>
<% else %>
<%= render 'page_not_found' %>
<% end %>
<% end %>
